# Core app
streamlit>=1.34
pandas>=2.2
numpy>=1.26
requests>=2.31
pydantic<2.0
python-dotenv>=1.0

# Modeling / ML
lightgbm>=4.1
joblib>=1.3
scikit-learn>=1.4      # lightgbm fits nicely alongside this

# Data sources & utils
espn-api>=0.37.0       # pip package name uses a hyphen
nfl_data_py>=0.3.3     # pulls weekly parquet from nflfastR
pyarrow>=15.0          # required by pandas.read_parquet used inside nfl_data_py
rapidfuzz>=3.6         # fuzzy roster name mapping

# Optimization
pulp>=2.8              # LP solver wrapper (needs CBC installed system-wide)

# HTML parsing (optional but handy for any scraping utilities you kept)
beautifulsoup4>=4.12
lxml>=5.2
